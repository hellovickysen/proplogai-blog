---
interface Props {
  title: string;
  description: string;
  date: Date;
  slug: string;
  tags?: string[];
  coverImage?: string;
}

const { title, description, date, slug, tags = [], coverImage } = Astro.props;
---

<a href={`/blogs/${slug}`} class="group block h-full">
  <article class="h-full flex flex-col rounded-2xl border border-white/[0.06] bg-white/[0.015] overflow-hidden hover:border-white/[0.12] transition-all duration-500">
    {coverImage && (
      <div class="aspect-[16/9] overflow-hidden">
        <img
          src={coverImage}
          alt={title}
          class="w-full h-full object-cover group-hover:scale-[1.03] transition-transform duration-700 ease-out"
          loading="lazy"
        />
      </div>
    )}

    <div class="p-6 flex-1 flex flex-col">
      {tags.length > 0 && (
        <div class="mb-3">
          <span class="font-mono text-[10px] uppercase tracking-[0.15em] text-violet font-medium">
            {tags[0]}
          </span>
        </div>
      )}

      <h3 class="font-display text-[1.05rem] font-semibold text-white leading-snug mb-2.5 group-hover:text-violet/90 transition-colors duration-300 line-clamp-2">
        {title}
      </h3>

      <p class="text-[13px] text-white/40 leading-relaxed line-clamp-2 flex-1">
        {description}
      </p>
    </div>
  </article>
</a>
